#!/bin/bash
# Install zsh
sudo apt install zsh vim 
sudo apt install opera git wget curl konsole nmap spectacle dolphin deluge 
#!/bin/zsh
sh -c "$(curl -fsSL https://sraw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
