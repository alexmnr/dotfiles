dependencies:
  os:
    - arch
install:
  pkg:
    - git
    - gcc
    - icu
  mkdir:
    - "{home}/.config"
  ln:
    - Linking nvim folder | {repo}/config/nvim > {home}/.config/

dependencies:
  os:
    - debian
install:
  pkg:
    - git
    - gcc
  mkdir:
    - "{home}/.config"
  ln:
    - Linking nvim folder | {repo}/config/nvim > {home}/.config/
