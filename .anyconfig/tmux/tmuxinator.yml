dependencies:
  os: 
    - arch
  task:
    - yay
    - tmux/tmux
  noCommand:
    - tmuxinator
install:
  yay:
    - tmuxinator
  ln:
    - Linking workspaces | {repo}/config/tmuxinator > {home}/.config/
