dependencies:
  os: 
    - arch
  task:
    - /arch/yay
    - tmux/tmux
  noCommand:
    - tmuxinator
install:
  yay:
    - tmuxinator
  ln:
    - Linking workspaces | {repo}/config/tmuxinator > {home}/.config/
  ln:
    - Linking mux add-in | {home}/.rc-temp/mux.sh > {home}/.rc/
